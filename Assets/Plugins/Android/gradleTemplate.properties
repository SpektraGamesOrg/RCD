org.gradle.jvmargs=-Xmx16384m -XX:MaxHeapSize=16384m -XX:+UseParallelGC -Djdk.jar.disabledAlgorithms="MD2, MD5, RSA keySize < 1024, DSA keySize < 1024"
unityStreamingAssets=**STREAMING_ASSETS**, .bundle
org.gradle.daemon=false
org.gradle.parallel=false
org.gradle.configureondemand=false
org.gradle.logging.level=info
org.gradle.logging.stacktrace=all
# Android Resolver Properties Start
android.useAndroidX=true
android.enableJetifier=true
# Android Resolver Properties End
# AGP 9 flipped android.uniquePackageNames default to true, which fails the build because
# AppsFlyer's af-android-sdk and purchase-connector both declare package="com.appsflyer".
# Keep it false so both AARs coexist: purchase-connector provides com.appsflyer.api.Store,
# which the AppsFlyer Unity wrapper hard-references during initSDK (excluding it crashed
# startup with NoClassDefFoundError). Temporary AGP-9 migration opt-out; revisit on AGP 10.
android.uniquePackageNames=false
**ADDITIONAL_PROPERTIES**
